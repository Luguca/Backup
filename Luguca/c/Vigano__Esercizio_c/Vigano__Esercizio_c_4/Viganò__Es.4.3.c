#include <stdio.h>
int main(void)
{
    int n, o, c, s;

    c=0;
    printf("Numero: ");
    scanf("%d", &n);
    while (s!=10)
    {
        while (c!=2)
    {
        s=s+n;
        c=c+1;
        printf("Numero: ");
        scanf("%d", &n);
    }
    if(s!=10)
    {
        printf("rifai");
    }
    else
    {

    }
    }
    
}
